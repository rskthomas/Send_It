<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/drawingFragment">

    <fragment
        android:id="@+id/drawingFragment"
        android:name="com.buggysoft.sendit.DrawingFragment"
        android:label="fragment_drawing"
        tools:layout="@layout/fragment_drawing" >
        <action
            android:id="@+id/action_drawingFragment_to_pairingFragment"
            app:destination="@id/pairingFragment" />
    </fragment>
    <fragment
        android:id="@+id/pairingFragment"
        android:name="com.buggysoft.sendit.PairingFragment"
        android:label="fragment_pairing"
        tools:layout="@layout/fragment_pairing" />
</navigation>